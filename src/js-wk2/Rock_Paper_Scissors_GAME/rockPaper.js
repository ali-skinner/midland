"use scrict";

const userSelection = prompt("Pick one: rock, paper, scissors (no lizard, Spock in this beta version?");
const rock = 0;
const paper = 1;
const scissor = 2;
const lizard = "nice try sucka";
const Spock = "Sheldor wins";
const counter = counts all the games  // should this be an array? if yes should it be a const or a let



// make a scoreboard - running counter to track wins/losses
// keep track of the game count - how many games have been played
// a count for function;
// wrtite a math.random to generate the computer random choice or 0,1,2 - used index values for rock, paper, scissors and return that value from the function;
// buttons need eventlisteners and on click actions - do a concole log on click to make sure it works
// refactor event listeners to have a message when the button is clicked

// buyApple.addEventListener('click',e=>{
//     // console.log(fruitPrice/2);
//     const applesBought=fruitPrice/2;
//     displayMsg.innerHTML=`<h2>You've bought ${applesBought} item</h2>`

//     if (fruitPrice/2>1){
//         displayMsg.innerHTML=`<h2>You've bought ${applesBought} items</h2>`
//     }

//     const price=document.getElementById('price');
//     price.innerHTML=`<h2>Price:$${fruitPrice}</h2>`;


// });